export interface CashRequest {
    id_cash: number,
    duedate: Date,
    appr_date: Date,
    jumlah: number,
    peruntukan: string,
    penerima: string,
    norek: string,
    status: number,
    referensi: string,
    divisi: string
}
